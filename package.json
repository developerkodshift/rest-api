{
  "name": "shopify-rest-api",
  "version": "1.0.0",
  "description": "Node.js REST API server that wraps Shopify REST Admin API for product listing, filtering, details, measurements, size charts, and images",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
